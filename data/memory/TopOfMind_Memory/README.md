# TopOfMind Memory

This folder holds actual memory files.

SQLite indexes the files. The Hub API enforces permissions. Folder names are
human-friendly organization, not the security boundary.

Each bucket should have a `_bucket.json` file.

