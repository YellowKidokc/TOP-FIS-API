Installation Readme

To install the scanner service, unzip the package and run the setup
executable. Configure the database connection string and the worker
count. The installer registers a background service that processes
documents and writes results to the catalog. See the troubleshooting
section for permission and network errors.
